# Repository Guidelines

## Git 작업 흐름

### 새 작업 시작

새 작업을 시작하기 전에는 반드시 다음 순서를 따른다.

1. 현재 브랜치와 작업 트리 상태를 확인한다. 사용자 변경이나 미추적 파일은 수정·복구·스테이징·삭제하지 않는다.
2. `main` 브랜치로 전환한다.
3. `git pull --ff-only origin main`으로 원격 `main`을 최신화한다.
4. 최신 `main`에서 `codex/<작업명>` 형식의 새 브랜치를 만든다.
5. 사용자가 명시적으로 요청하지 않은 한 `main`에 직접 커밋하지 않는다.

작업 트리가 더러워 브랜치 전환 또는 pull이 안전하지 않으면, 우회하거나 덮어쓰지 말고 상태와 영향을 보고한 뒤 지시를 받는다.

### 커밋 규칙

- 한 커밋에는 하나의 독립적이고 검증 가능한 변경만 담고, 그 변경을 검증하는 테스트도 함께 포함한다.
- 관련 없는 리팩터링, 포맷 변경, 사용자 변경은 같은 커밋에 섞지 않는다.
- 스테이징 전 전체 상태를 확인하고, `git add -- <명시적 경로>`로 이번 작업 파일만 추가한다. `git add .`, `git add -A`, `git add --all`은 사용하지 않는다.
- 커밋 전에는 `git diff --staged`와 관련 테스트·빌드 결과를 확인하고, 비밀값·로컬 설정·생성물이 없는지 검토한다.
- 커밋 제목은 다음 형식을 사용한다.

  ```text
  <type>(<optional-scope>): <구체적인 한국어 결과 문장>
  ```

- `type`은 주된 목적에 따라 `feat`, `fix`, `docs`, `refactor`, `test`, `build`, `ci`, `perf`, `style`, `chore` 중 하나를 쓴다. `scope`는 의미를 분명하게 할 때만 소문자 영문으로 쓴다.
- 제목은 짧고 단독으로 이해 가능하게 쓴다. `수정`, `업데이트`, `버그 수정`, `Phase 1`처럼 결과를 알 수 없는 표현은 쓰지 않는다.
- 변경 이유·트레이드오프·위험·검증이 중요한 경우에는 빈 줄 뒤 본문에 사실만 기록한다. 호환성을 깨는 변경은 `!` 또는 `BREAKING CHANGE:`를 명시한다.

### PR 제목과 본문 규칙

- PR은 최신 `main`을 대상으로 만들고, 실제 브랜치 diff와 커밋만 근거로 작성한다.
- 제목은 짧고 결과 중심으로 쓴다. 기본 형식은 커밋과 같은 `<type>(<scope>): <결과>`이며, 제목만 읽어도 무엇이 달라졌는지 알 수 있어야 한다.
- 본문은 아래 순서를 사용한다. 실행하지 않은 검증은 통과한 것처럼 쓰지 않는다.

  ```md
  ## 목적

  어떤 문제를 왜 해결하는지 설명합니다.

  ## 변경 내용

  - 실제로 바뀐 핵심 동작과 범위

  ## 검증

  - 실행한 명령과 결과
  - 실행하지 못한 검증과 이유

  ## 위험 및 배포 고려사항

  - 롤백 방법, 데이터 영향, 설정·권한·마이그레이션 주의점

  ## 리뷰 가이드

  - 먼저 볼 파일·흐름과 특히 확인할 결정 사항
  ```

- 본문은 파일 목록을 나열하는 대신 문제, 접근 방식, 결과, 검증 근거를 설명한다. 큰 변경은 가능한 한 독립적인 PR로 나누고, 관련 테스트는 같은 PR에 포함한다.
- PR 생성 전에는 스스로 diff를 검토해 의도치 않은 변경과 비밀값 노출이 없는지 확인한다.

### PR 병합과 브랜치 정리

1. PR을 생성한 뒤 필수 테스트·빌드·CI·리뷰·병합 가능 상태를 확인한다.
2. 사용자가 병합을 요청했거나 저장소 정책이 허용할 때만 병합한다.
3. 실제 PR이 `MERGED` 상태이고 원격 `main`이 병합 커밋을 가리키는지 확인한다.
4. `main`으로 전환해 `git pull --ff-only origin main`을 실행한다.
5. 작업 트리에 미커밋 변경이 없고 작업 브랜치를 사용하는 프로세스나 worktree가 없는지 확인한다.
6. 확인이 끝난 작업 브랜치만 `git branch -d <branch>`로 로컬에서 삭제하고, `git push origin --delete <branch>`로 원격에서도 삭제한다.
7. `git fetch --prune`으로 삭제된 원격 참조를 정리한다.

브랜치가 병합되지 않았거나, 미커밋 변경·사용 중인 프로세스·연결된 worktree가 있으면 삭제하지 말고 사용자에게 상태를 보고한다.
